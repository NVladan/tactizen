{
  "name": "tactizen-blockchain",
  "version": "1.0.0",
  "description": "Blockchain integration for Tactizen NFT system",
  "scripts": {
    "deploy:zen": "hardhat run scripts/deploy-zen.js --network baseSepolia",
    "deploy:nft": "hardhat run scripts/deploy-nft.js --network baseSepolia",
    "verify:zen": "hardhat verify --network baseSepolia",
    "verify:nft": "hardhat verify --network baseSepolia"
  },
  "dependencies": {
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.0",
    "zkverifyjs": "^0.7.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.4"
  }
}
